<?php
    class Auth extends Database {
        public function login () {
            echo "let's login here";
            // $sql = "SELECT * FROM users";
            // $stmt = $this->connect()->prepare($sql);
            // $stmt->execute();
            // $users = $stmt->fetchAll(PDO::FETCH_ASSOC);
            // $this->closeConnection();
            // return $users;
        }
    }
?>