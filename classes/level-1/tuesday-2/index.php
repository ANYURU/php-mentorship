<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tuesday2</title>
</head>
<body>
    
    <?php //echo "100 - 80 =", 100 - 80?>
    <?php 
    /**
     * Statement: A statement is a line of code that is made up of keywords, identifiers and expressions.
     * Can be an assignment, a function call a loop and a conditional.
     * Is a line of code that performs a specific task. 
     * A command that performs a specific task. 
     * 
     * Statement: A valid line of code that is made up of an expression or expressions. 
     * Example
     * a = 1; Expression. It is a valid statement because it is composed of an expression.
     * b = 4;
     * c = a + b;
     * print("hello world");
     * 
     * Expression: An expression is a statement that is made up of operands and operators to carry out a specific operation.
     * For example a function call is not an expression because it doesnot specify operands and operators unless if a function call is assigned to a variable.
     * 
     * Operators: 
     * Operand: These are values to which we apply an operation is applied.
     * 1 + 10 - 2 * 4 In this line of code 1, 2, 10 and 4 are the operands while the +, - and * are the operators. 
     * Assignment: An assignment is an association between an value and an identifier (variable).
     * Initialisation: An assignment of an initial value to a variable or identifier. Creating a variable and at the same time setting the first value to it. 
     * name = 'David'
     * 
     * 
     * www.dpaste.org for sharing single file code snippets. 
     */
    ?>
</body>
</html>